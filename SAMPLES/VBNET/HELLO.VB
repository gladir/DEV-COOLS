' HELLO.VB - Hello World en Visual Basic .NET
' Exemple de base pour VBNCW32

Module MainModule
    Sub Main()
        Dim sMessage As String
        sMessage = "Hello, World!"
        Console.WriteLine(sMessage)
    End Sub
End Module
