╔══════════════════════════════════════════════════════════════════════════════╗
║                      TODO 15 IMPLEMENTATION INDEX                            ║
║              Complete Reference for GOTO and LABELS Feature                 ║
╚══════════════════════════════════════════════════════════════════════════════╝

GENERATED DOCUMENTS:
═════════════════════════════════════════════════════════════════════════════

📄 TODO15_QUICK_SUMMARY.txt (13 KB)
   ├─ What you need to implement (5 main features)
   ├─ Existing infrastructure (symbol table, constants, etc.)
   ├─ Required code changes (5 specific modifications)
   ├─ Testing checklist (6 test cases)
   ├─ Assembly output examples
   └─ Key points summary
   
   START HERE for implementation!

📄 TODO15_REFERENCE.md (24 KB)
   ├─ Full TODO 15 specification (lines 224-236)
   ├─ All constant definitions
   ├─ Global variables
   ├─ Forward declarations
   ├─ Complete symbol table function implementations
   ├─ Label generation details
   ├─ All parsing functions
   ├─ Keyword recognition
   ├─ GlobalInit procedure
   ├─ Run procedure
   └─ Implementation summary
   
   USE FOR detailed reference and context

📄 TODO15_INDEX.txt (this file)
   └─ Navigation guide for all materials

═════════════════════════════════════════════════════════════════════════════

SOURCE FILE SECTIONS:
═════════════════════════════════════════════════════════════════════════════

AL60C86.PAS - Lines referenced in implementation:

SPECIFICATION & CONSTANTS:
  Line 224-236   → TODO 15 specification
  Line 432       → MAX_LABEL constant (limit for symbol table)
  Line 442-450   → ASM naming prefixes (A60_LBL_PFX = '_A60L_')
  Line 458-467   → Type index constants (TY_IDX_LABEL = 4)
  Line 539       → Symbol kind constants (SK_LABEL)
  Line 560-569   → Type kind constants (TYK_LABEL)

GLOBAL STATE:
  Line 616       → LabelSeq variable (counter for generated labels)

FORWARD DECLARATIONS:
  Line 690       → EmitLabel procedure declaration
  Line 700-750   → All forward declarations
  Line 732       → EmitExprRuntime declaration

LEXER & KEYWORDS:
  Line 1050-1069 → IsKeyword function (recognizes 'GOTO', 'GO', 'TO')
  Line 1062      → GOTO keyword recognition line

LABEL GENERATION:
  Line 1612-1620 → LabelGen() function (generates _A60L_1, _A60L_2, ...)

EMIT LABEL:
  Line 1956-1959 → EmitLabel(Lbl) procedure (outputs label to assembly)

INITIALIZATION:
  Line 992       → LabelSeq := 0 in GlobalInit
  Line 1000-1031 → GlobalInit procedure (symbol table initialization)

SYMBOL TABLE FUNCTIONS:
  Line 2596-2603 → SymInit() - Initialize symbol table
  Line 2608-2621 → SymNameClean() - Sanitize identifier for MASM
  Line 2631-2647 → SymMangle() - Build ASM label for symbol
  Line 2652-2665 → SymFind() - Search symbol by name
  Line 2669-2684 → SymFindInScope() - Search in specific scope
  Line 2689-2715 → SymAdd() - Add symbol to table
  Line 2725-2736 → SymEnterScope() - Enter new scope level
  Line 2741-2751 → SymLeaveScope() - Leave current scope

PARSING FUNCTIONS:
  Line 2340-2420 → ParsePrimary() - Handle identifiers
  Line 3162-3215 → ParseExprStmt() - Parse expressions/assignments
  Line 3222-3251 → ParseStmt() - Statement dispatcher (MODIFY HERE)
  Line 4187-4202 → ParseForStmt() - Example of scope handling
  Line 4270-4386 → Run() - Main parse loop (MODIFY HERE)

═════════════════════════════════════════════════════════════════════════════

IMPLEMENTATION ROADMAP:
═════════════════════════════════════════════════════════════════════════════

STEP 1: Read documentation
  → Read TODO15_QUICK_SUMMARY.txt first
  → Reference TODO15_REFERENCE.md for detailed context

STEP 2: Understand current state
  → Labels already in symbol table as SK_LABEL (line 539)
  → Label type already defined as TYK_LABEL (line 564)
  → Label prefix already defined as A60_LBL_PFX (line 447)
  → EmitLabel() procedure already exists (line 1956)
  → SymAdd/SymFind functions already exist
  
STEP 3: Add forward declaration
  → Add "Procedure ParseGotoStmt; Forward;" around line 740

STEP 4: Implement ParseGotoStmt
  → New procedure to handle GOTO statements
  → Check symbol table for label
  → Emit JMP instruction

STEP 5: Handle label declarations
  → Modify ParseExprStmt to recognize "label:" pattern
  → Add to symbol table as SK_LABEL
  → Emit label in assembly

STEP 6: Update ParseStmt dispatcher
  → Add case for GOTO keyword (line 3250)
  → Add case for GO keyword (line 3250)

STEP 7: Update Run main loop
  → Replace GOTO/GO skip with proper parsing (line 4356)

STEP 8: Test all cases
  → Simple labels
  → GOTO statements
  → GO TO variant
  → Block scoping
  → Error conditions

═════════════════════════════════════════════════════════════════════════════

QUICK REFERENCE - KEY LINES:
═════════════════════════════════════════════════════════════════════════════

For label declarations (SK_LABEL):
  ├─ Kind: SK_LABEL (line 539)
  ├─ Type: TY_IDX_LABEL = 4 (line 462)
  ├─ Mangled: A60_LBL_PFX + name = '_A60L_' + name (line 447)
  └─ Usage: SymAdd(name, SK_LABEL, TY_IDX_LABEL, 0)

For GOTO statements:
  ├─ EmitInstr('JMP', '_A60L_label')
  ├─ EmitInstr('JMP', 'BX')  [for indirect via switch]
  └─ Parsing: ParseStmt (line 3250) or Run (line 4356)

For internal labels:
  ├─ LabelGen() generates _A60L_1, _A60L_2, ... (line 1612)
  ├─ Uses LabelSeq counter (line 616)
  └─ Already used for IF/FOR branches

═════════════════════════════════════════════════════════════════════════════

TESTING STRATEGY:
═════════════════════════════════════════════════════════════════════════════

Basic tests (in order of complexity):

1. Compiler infrastructure test:
   BEGIN
     x := 1;
   END.
   
2. Label only (no jump):
   BEGIN
     mystart: x := 1;
   END.

3. Simple GOTO:
   BEGIN
     x := 0;
     loop: x := x + 1;
     IF x < 3 THEN GOTO loop;
   END.

4. GO TO variant:
   BEGIN
     y := 0;
     check: y := y + 1;
     IF y < 2 THEN GO TO check;
   END.

5. Block scoping:
   BEGIN
     inner: z := 1;
     BEGIN
       a := 1;
     END;
   END.

6. Error cases:
   GOTO undefined;  (should error - label not found)

═════════════════════════════════════════════════════════════════════════════

IMPLEMENTATION TIME ESTIMATE:
═════════════════════════════════════════════════════════════════════════════

Reading & Understanding:     15 minutes
  → Read QUICK_SUMMARY
  → Review key source lines
  → Understand symbol table usage

Implementation:              45-60 minutes
  → Add forward declaration: 2 minutes
  → Implement ParseGotoStmt: 20 minutes
  → Handle label declarations: 15 minutes
  → Update ParseStmt: 5 minutes
  → Update Run: 5 minutes

Testing:                     30 minutes
  → Basic test cases
  → Error conditions
  → Scope validation

TOTAL: ~90-105 minutes (1.5-2 hours)

═════════════════════════════════════════════════════════════════════════════

HELP & DEBUGGING:
═════════════════════════════════════════════════════════════════════════════

If labels not being recognized:
  → Check IsKeyword() includes 'GOTO', 'GO', 'TO' (line 1062)
  → Verify SK_LABEL is in symbol kinds enum (line 539)
  → Check TY_IDX_LABEL is properly defined (line 462)

If GOTO not jumping:
  → Verify EmitLabel() is called for label definitions (line 1956)
  → Check SymAdd() is called with correct SK_LABEL kind
  → Ensure EmitInstr('JMP', label) generates correct ASM
  → Verify label is in symbol table at parse time

If scope issues:
  → Check SymEnterScope/SymLeaveScope called for BEGIN/END (lines 2725, 2741)
  → Verify label scope level matches jump scope level
  → Labels should only be visible in their scope and child scopes

═════════════════════════════════════════════════════════════════════════════

REFERENCES IN AL60C86.PAS:
═════════════════════════════════════════════════════════════════════════════

Symbol kind enum (TSymKind):
  SK_VAR, SK_PARAM, SK_PROC, SK_LABEL, SK_SWITCH, SK_ARRAY, SK_OWN

Type kind enum (TTypeKind):
  TYK_INTEGER, TYK_REAL, TYK_BOOLEAN, TYK_STRING, TYK_LABEL,
  TYK_SWITCH, TYK_PROCEDURE, TYK_ARRAY, TYK_UNKNOWN

Symbol table record (TSymRec):
  Name, MangledName, Kind, TypeIdx, ScopeLevel, Offset, IsOwn, IsValue,
  IsName, DeclLine

Scope management:
  SymCount, ScopeStack[], ScopeTop, CurScope, CurFrameSize

Label generation:
  LabelSeq counter incremented in LabelGen()

═════════════════════════════════════════════════════════════════════════════

FINAL CHECKLIST BEFORE SUBMISSION:
═════════════════════════════════════════════════════════════════════════════

Code changes:
  □ Forward declaration added
  □ ParseGotoStmt implemented
  □ Label declaration parsing added
  □ ParseStmt updated with GOTO/GO cases
  □ Run procedure updated
  □ Code compiles without errors
  □ No syntax errors

Functionality:
  □ Simple labels can be declared
  □ GOTO jumps to labeled positions
  □ GO TO works as synonym
  □ Labels are scoped to blocks
  □ Appropriate errors for undefined labels
  □ Assembly output looks correct

Testing:
  □ All test cases pass
  □ Error cases handled properly
  □ Assembly can be assembled with MASM

═════════════════════════════════════════════════════════════════════════════

Created: March 14, 2025
For: AL60C86.PAS - TODO 15 Implementation
Status: Ready for implementation

═════════════════════════════════════════════════════════════════════════════
